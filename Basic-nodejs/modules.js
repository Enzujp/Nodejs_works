// const { people, ages } = require('./people');

// // Imports people file from the same directory and then it also takes out the specified parameters from the imported file

// // Importing a file doesnt give automatic access to the file

// console.log(people, ages);

// const os = require('os');

// console.log(os.platform(), os.homedir());

const {people, ages} = require('./people');

console.log(people, ages);