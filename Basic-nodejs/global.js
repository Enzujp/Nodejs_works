// console.log(global);

// setTimeout(() => {
//     console.log('in the timeout');
//     clearInterval(int)
// }, 3000);


// const int = setInterval(() => {
//     console.log('in the interval')
// }, 1000);

// console.log(__dirname);

// used to determine filepath

// console.log(__filename);


// you can call some constant functions using the global inbuilt function

setTimeout(() => {
    console.log('in the fire');
    clearInterval(int);

}, 3000);

const int = setInterval(() => {
    console.log('in the interval')    
}, 1000);

// The interval time passsed determines the time required to go by before the message is displayed